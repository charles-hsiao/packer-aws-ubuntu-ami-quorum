check process logstash matching "/usr/bin/java*logstash*" every 5 cycles
  start program = "/bin/su -c 'service logstash start'"
  stop program = "/bin/su -c 'service logstash stop'"
  if does not exist then restart
